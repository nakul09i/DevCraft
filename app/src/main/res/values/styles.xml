<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.DevCraft" parent="android:Theme.Material.Light.NoActionBar" />

    <!--
        Applied to MainActivity so the cold-start window is branded rather than
        blank white. MainActivity swaps back to Theme.DevCraft in onCreate.
    -->
    <style name="Theme.DevCraft.Launch" parent="Theme.DevCraft">
        <item name="android:windowBackground">@drawable/brand_launch_background</item>
    </style>
</resources>
